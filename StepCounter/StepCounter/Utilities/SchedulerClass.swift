import Foundation
import UIKit

class Scheduler {
    // func query() queries the data to save at the end of each day 11:59pm/ when the app enters foreground and call function in app did enter delegate
    static func queryDataAtTheEndOfTheDay() {
        // create titmer
        var timer = Timer.scheduledTimer(withTimeInterval: 1, repeats: true) {
            timer in
            
        }
    }
    
  
}
